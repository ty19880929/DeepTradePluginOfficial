"""Dataclass / Pydantic schemas for Checkmate.

Iter-0 PR-0.1 placeholder: importable empty module so ``plugin.validate_static``
has a static target. Real schemas (UniverseSnapshot, FeaturesRow, RegimeRow,
Signal, Position, Trade, BacktestRun, ReportPayload) arrive in Iter-1+.
"""

from __future__ import annotations
